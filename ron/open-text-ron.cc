#include "ron/open-text-ron.hpp"

using namespace std;

namespace ron {

    int64_t OpenTextFrame::Cursor::integer(fsize_t idx) {

    }

    double OpenTextFrame::Cursor::number(fsize_t idx) {
    }

    std::string OpenTextFrame::Cursor::string(fsize_t idx) {

    }

    Uuid OpenTextFrame::Cursor::uuid(fsize_t idx) {

    }


}